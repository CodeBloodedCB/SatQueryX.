# Optional local override. Production defaults to the deployed FastAPI URL.
# VITE_API_URL=http://localhost:8000

# If you deploy your own backend, set this to its public origin (no /api suffix).
# VITE_API_URL=https://your-backend.onrender.com
